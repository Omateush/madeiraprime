# Backend URL for production — set this in Vercel's dashboard under Environment Variables.
# Leave unset for local development; Vite's dev proxy handles /api/* requests automatically.
VITE_API_URL=https://your-app.onrender.com
